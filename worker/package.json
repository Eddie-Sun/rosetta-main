{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "kv:list": "wrangler kv:key list --binding=ROSETTA_CACHE",
    "secret:set": "wrangler secret put FIRECRAWL_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.114.15"
  }
}
