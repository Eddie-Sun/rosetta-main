# Copy these into `dashboard/.env.local` (do not commit .env.local).
#
# Neon provides both pooled and direct connection strings.
# Prisma v7 reads connection URLs from `prisma.config.ts`, not `schema.prisma`.
#
# For fastest setup, use the direct connection string here (works for migrations + runtime).
# If you later want pooled connections in runtime, we can switch to a Neon adapter.
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/DB?sslmode=require"


